# Core Django
Django>=5.2.0,<6.0
djangorestframework>=3.15.0,<4.0
djangorestframework-simplejwt>=5.3.0,<6.0
django-cors-headers>=4.0.0,<5.0

# Environment & config
python-dotenv>=1.0.0,<2.0

# PostgreSQL adapter (Task 1.1 - production DB)
psycopg2-binary>=2.9.0,<3.0

# ASGI server
uvicorn>=0.30.0,<1.0

# Redis (for chat, caching, Celery)
redis>=5.2.0,<6.0

# PDF generation (Task 3.1 - payslip PDF, reports)
weasyprint>=60.0,<61.0

# Async task queue (Task 3.3 - payroll batch processing)
celery>=5.3.0,<6.0
django-celery-results>=2.5.0,<3.0
